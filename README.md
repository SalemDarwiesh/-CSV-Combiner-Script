# -CSV-Combiner-Script
This Python script iterates through a directory structure, combines CSV files within each subfolder, and saves the combined data into a new CSV file with a modified name.  It's designed to handle daily CSV reports and consolidate them into a single daily summary file.
